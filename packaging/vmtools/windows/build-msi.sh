#!/usr/bin/env bash
# Windows MSI scaffold — requires WiX Toolset on a Windows build host.
set -euo pipefail
echo "Windows MSI packaging is not yet implemented."
echo "Use virtio-win via Zeus OS Guest Tools for Windows VMs."
exit 1
